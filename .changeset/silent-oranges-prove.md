---
'@jbpark/live-editor': minor
---

Add support for editing `innerHTML` bindings in the panel and applying updates through the AST pipeline. Introduce optional provider-based wrapping in DnD rendering to improve preview integration flexibility.
